## CodeFolio Pro Setup

**Requirements:** Flutter SDK ≥ 3.0 installed and in PATH.

```powershell
cd "c:\CodeFolio Pro"
flutter pub get
flutter run
```

To build a debug APK:
```powershell
flutter build apk --debug
# Output: build\app\outputs\flutter-apk\app-debug.apk
```
